server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Bregandert/Conf.git
          search-paths: myconfig
          clone-on-start: true
        label: master
  application:
    name: config_server

logging:
  level:
    org.springframework.cloud: DEBUG

#    https://github.com/bolshakov-coach/springcloud-config/tree/master/springleveltwo

